program exemplo

proc main
    // soma( 2, 7 );
    pushiv 2
    pushiv 7
    pcall soma

    pushiv 10
    pushiv 20
    pcall soma

    exit_0
endproc

proc soma
    param x int
    param y int

    var t1 int

    pushsv "A soma é: "
    pushst
    push_1
    lcall imprima

    isum t1, x, y
    pushiv t1
    pushit
    push_1
    lcall imprima

    decsp 12
    ret	
endproc

endprogram

