program exemplo

var v bool

proc main
    iset v, true
    iset v, false
    iset v, v

    pushiv true
    pushlt
    pushiv 1
    lcall imprima

    pushiv false
    pushlt
    pushiv 1
    lcall imprima

    pushiv v
    pushlt
    pushiv 1
    lcall imprima

    exit 0
endproc

endprogram

