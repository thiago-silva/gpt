program exemplo

var v bool

proc main
    iset v, true
    iset v, false
    iset v, v

    pushiv true
    pushbt
    pushiv 1
    libcall imprima

    pushiv false
    pushbt
    pushiv 1
    libcall imprima

    pushiv v
    pushbt
    pushiv 1
    libcall imprima

    exit 0
endproc

endprogram

