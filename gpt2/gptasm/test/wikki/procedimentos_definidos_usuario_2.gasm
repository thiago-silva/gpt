program exemplo

var s int

proc main
    // soma( s, 2, 7 );
    pushiv s
    pushiv 2
    pushiv 7
    pcall soma
	
    // imprima( s );
    pushiv s
    pushit
    push_1
    lcall imprima
	
    exit_0
endproc

proc soma
    param r int
    param v1 int
    param v2 int
    var t1 int

    // r := v1 + v2;
    isum t1, v1, v2
    isetv r:0, t1

    decsp 16
    ret	
endproc

endprogram

