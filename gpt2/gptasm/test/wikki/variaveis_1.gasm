program exemplo

var x int
var y int

proc main
    var t1 int
    var t2 int

    // x := 10;
    iset x, 10
	
    // y := 20;
    iset y, 20
	
    // imprima( "x*y+4=", x*y+4 );
    imul t1, x, y
    isum t2, t1, 4
    pushiv t2
    pushit
    pushsv "x*y+4="
    pushst
    pushiv 2
    libcall imprima
	
    exit 0
endproc

endprogram

