program exemplo

var x int

proc main
    var t1 int

    // para x de 1 até 10 faça
    iset x, 1
    teste:
    ile t1, x, 10
    ifnot t1, proximo

    // imprima( x );
    pushiv x
    pushit
    push1
    libcall imprima

    // fim-para
    iinc x, 1
    jmp teste

    proximo:

    exit0
endproc

endprogram

