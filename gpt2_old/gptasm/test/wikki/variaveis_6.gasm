program exemplo

var v bool

proc main
    iset v, true
    iset v, false
    iset v, v

    pushiv true
    pushbt
    pushiv 1
    lcall io, imprima

    pushiv false
    pushbt
    pushiv 1
    lcall io, imprima

    pushiv v
    pushbt
    pushiv 1
    lcall io, imprima

    exit 0
endproc

endprogram

